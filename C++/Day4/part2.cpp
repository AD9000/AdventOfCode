#include <iostream>
#include <climits>
using namespace std;

#define N 150010
#define FASTIO                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(0);                       \
    cout.tie(0);
#define INF INT_MAX
#define pb push_back
#define x first
#define y second
typedef long long ll;
typedef pair<ll, ll> pii;

int main()
{
    FASTIO;

    int n, m;
    cin >> n >> m;

    return 0;
}
